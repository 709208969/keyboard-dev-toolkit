/**
 * 立创 EDA 导出 — Pro 占位实现（stub）
 *
 * ⚠ 本文件是公开仓的占位实现，真实实现位于私有仓 keyboard-editor-pro，
 *   由 `node scripts/fetch-pro.mjs` 注入覆盖。禁止在此添加任何业务逻辑。
 */

/** Pro 版专属：真实实现在私有仓 */
export function generateLCEDAReadme(_projectName?: string): string {
  throw new Error('立创 EDA 导出为专业版功能：请使用专业版构建（fetch-pro 注入后可用）');
}
