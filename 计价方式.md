# 计价方式 — PCBA 计价模块交接文档

> **给接手模型的必读文档。** 本文件记录 2026-08-27 当天完成的 PCBA 计价模块全部情况：
> 需求原始原文、定稿规则、架构实现、踩坑决策、验证方式、待办事项。
> 用户（K星工作室 kevin）会持续调整计价方式，新会话先读本文件再动手。

---

## 0. 今日变更日志（2026-08-28，v2.6.0 → v2.7.0）

| 版本 | 变更 |
|------|------|
| v2.7.0 | 定位板报价：**尺寸支持手动修改**（0 = 跟随定位板编辑器，「从PCB取尺寸」按钮恢复跟随） |
| v2.7.0 | 定位板**计价乘数 ×2.5**（`plate.multiplier`，仅定位板，PCB 的 terminalMultiplier=2 不变；终端价 = 成本 × 乘数） |
| v2.7.0 | 定位板**加工费下限 300 元/单**（`plate.minProcessFee`：加工费 = max(张数×加工费率, 300)） |
| v2.7.0 | plate 区 + pcb 区各加**人工报价按钮 Discord / Email**（`contacts` 配置：Discord 跳转 kindlestarkevin 账户、Email 跳转 X709208969@gmail.com） |
| v2.7.0 | **定位板报价默认折叠**，点击展开（与 PCB 报价页交互一致） |
| v2.7.0 | **页面隐藏全部成本构成**：PCB 价格卡去掉板费/工艺费/逐项明细(+¥)/成本小计/终端倍率/不计倍率行，只留单价/总价/模式/用板/报废/尺寸；表单所有加价小字(+N/PCS、+N/单)去掉；定位板只留单价/总价 |
| v2.7.0 | **复制文本彻底精简**：去掉板费/工艺费/成本小计/乘数行，保留配置项与总价/单价 |
| 代码 | 编辑器 v1.0.38；calculatePlateQuote 加 multiplier/minProcessFee 参数（缺省 1/0 向后兼容）；PlateQuote 增 totalPrice/multiplier 字段 |

### 昨日日志（2026-08-28，v2.5.0 → v2.6.0）

| 版本 | 变更 |
|------|------|
| v2.6.0 | **六项计费定稿**：额外小板（板载USB+5/C5+10/S3+10/自定义+15 元/PCS，单选默认无）+ 线长输入（≤100mm，板载USB时禁用）+ 排线（黑色0/FPC+1 元/PCS）+ 自定义固件（灯效+400/网页+400 按单可叠加/其他设备仅提示）+ 走线（圆角/斜角0，定制+500 设计费）+ 定位板独立报价（复用 PCB 板材逻辑，独立数量） |
| v2.6.0 | **行为变更**：所有"人工报价"选项（OSP/OLED/三防漆/定制其他设备）由**阻断报价**改为**仅提示不阻断**（黄色 notice，价格照常显示）；尺寸超限仍阻断 |
| v2.6.0 | 新 5 类费用全部 `noMultiplier`（不计入终端倍率 ×2）；定位板报价独立显示不并入 PCB 总价、不翻倍 |
| 代码 | 编辑器 v1.0.36（build 自动 bump）；`calculatePlateQuote()` 独立函数复用板材逻辑；PlatePricingSection 挂定位板编辑器下方 |

### 昨日日志（2026-08-27，v2.1.1 → v2.5.0）

| 版本 | 变更 |
|------|------|
| v2.5.0 | Logo 计价：无 +400/单、Kindlestar 0、自定义 +100/单 |
| v2.4.0 | 快递费：重量>8.5kg 改重货公式 `150+重量×4+9`，取消 300 元上限 |
| v2.3.0 | 终端倍率排除项 `noMultiplier`（蓝牙/2.4G/热插拔/旋钮/全检/抽检/包装/MCU/RGB/快递不计入 ×2） |
| v2.2.0 | 终端报价倍率 `terminalMultiplier=2` |
| 代码 | 尺寸只从 PCB 编辑器取（`computePCBBounds`）+ 自动跟随；keyCount 陈旧 bug 修复；旋转输入误判修复；N=0 拒绝；UI 单价突出 |

## 1. 来龙去脉（背景）

- **目标项目**：Keyboard Dev Toolkit（`K:\0AMAC\kle-editor\keyboard-editor-public`）
  - Next.js 16 / React 19 / TS 5 / Tailwind v4，单页应用，静态导出
  - 键盘配列编辑器（KLE 兼容）+ PCB/定位板生成 + AI 助手
  - 版本 v1.0.34（2026-08-27），`npm run check` / `npx vitest run` / `npm run build`
- **参考项目**：`K:\0AMAC\kindlestar-pcba-shopify计价版`（纯前端 PCBA 计价器，7 步表单+实时报价）
  - 其公式为"面积×单价"模式，经分析**不适用于键盘业务**（面积阈值 1㎡ 会导致 37 把后单价从 1350 跌到 9.45 元，差 114 倍），故未照搬，改为按开板方式计价
- **业务**：Kindlestar（K星）键盘 PCBA 定制报价，报价逻辑由用户口述，逐步定稿

## 2. 架构与文件地图

### 价格数据源（唯一权威）
- **GitHub 仓库 `709208969/kindlestar-pricing`**（main 分支，公开）
  - `pricing.json` ← **改价只改这里**，改完递增 `_meta.version` + 更新 `updatedAt` + push
  - 地址：https://github.com/709208969/kindlestar-pricing/blob/main/pricing.json
- **本地快照** `src/data/pricing.json`（发布时快照，离线兜底，与仓库保持同步）
- 当前版本：**v2.6.0**

### 源码文件
| 文件 | 职责 |
|------|------|
| `src/lib/pricing-types.ts` | zod schema（PricingConfig）+ 解析校验 |
| `src/lib/price-calculator.ts` | **计价引擎（纯函数）**：板材费/工艺费/固定成本/拒绝逻辑 + `calculatePlateQuote()` 定位板独立报价 |
| `src/lib/pricing-loader.ts` | 配置加载：localStorage 缓存 → 内嵌快照；更新链路 raw 优先 → jsDelivr 兜底；版本递增校验 + diff 预览 |
| `src/lib/pcb-export.ts` | `computePCBBounds()`：**PCB 成品板框尺寸唯一数据源**（计价「从 PCB 取尺寸」与 PCB 预览共用，含边距/旋转感知/组件扩展） |
| `src/components/PricingSection.tsx` | 页面底部独立区块 UI（表单 + 价格卡 + 更新按钮 + 水印）；v2.6.0 新增 ⑤ 小板/排线、⑥ 固件/走线区块 |
| `src/components/PlatePricingSection.tsx` | **v2.6.0 定位板独立报价**（尺寸跟随定位板编辑器 + 独立数量 + 材质单选 + 价格卡），挂载于 PlateSection 下方 |
| `src/components/PlateSection.tsx` | 定位板编辑器；底部挂载 `<PlatePricingSection plateSize={plateResult 尺寸} />` |
| `src/components/EditorPage.tsx` | 挂载 `<PricingSection layout rgbEnabled={projectPcbConfig.needLed} pcbSize={pcbBounds} />`（PCB 区块之后、Footer 之前）；`pcbBounds = useMemo(computePCBBounds(...))` |
| `src/lib/i18n.tsx` | `tb.tab.pricing` + `pricing.*` 约 80 个 key（en/zh/zh-TW，其余语言 fallback en） |
| `tests/price-calculator.test.ts` | 70 用例（板材/工艺/固定成本/拒绝/边界/旋转输入/N=0/终端倍率/排除项/快递重货/v2.6.0 附加组件/仅提示/定位板） |
| `tests/pricing-loader.test.ts` | 17 用例（更新链路 mock fetch/版本/diff/缓存） |
| `tests/pcb-export.test.ts` | +5 用例：computePCBBounds 与 generatePCB 一致/边距变化/空配列/旋转感知/组件扩展 |

### 数据流
```
用户编辑键盘配列 → 页面底部「生成价格清单」→ 表单（尺寸「从PCB取尺寸」：PCB 编辑器 computePCBBounds 成品板框）
  → calculatePrice(config, form) → QuoteResult
  → 价格卡：模式(大板/分料板)/张数/报废/总价/单价/明细/水印/notice
改价：GitHub pricing.json → 工具内「更新价格」→ 版本校验+diff 确认 → localStorage 缓存
```

## 3. 计价规则定稿（公式全文）

### 输入
- 尺寸 L×W（mm）：**默认自动跟随 PCB 编辑器成品板框**（`computePCBBounds`）；用户手动编辑输入框后用手动值；
  「从PCB取尺寸」按钮 = 清除手动值恢复跟随。form 内 0 表示"未手动填写"
- 数量 ≥5；非 5 倍数自动向上取整（`effectiveQty = ceil(qty/5)*5`，多出部分按报废仍计成本，价格卡显示报废数）
- 材质：FR4 / 黑芯板；层数仅 2 层；板厚仅 1.2/1.6mm（**不影响计价**）

### ① 板材费（核心）
**计费尺寸** = `max(L,W) × (min(L,W) + 5×2)`（辅助边永远加短边，上下各 5mm）
**出板数** `N = ⌊ 板材总面积 × 0.8 ÷ 计费面积 ⌋`（0.8 = 20% 浪费系数，向下取整）
**张数** `= ⌈ effectiveQty ÷ N ⌉`

| 模式 | 条件 | 板材 | 单价 | 加工费 | 板材费 |
|------|------|------|------|--------|--------|
| 大板 | ≥30 PCS | 1245×1041 | FR4 198.6 / 黑芯 294 | 294/张 | 张数×(板价+294) |
| 分料板 | <30 PCS | 487.5×310.7 | FR4 24.8 / 黑芯 36.75 | 36.75/张 | 张数×(料板价+36.75) |

**超限规则（仅小批量）**：L > 487.5×0.8=390mm 或 W > 310.7×0.8=248.56mm → **拒绝自动计价**，提示联系客服

### ② 工艺费（全部相加）
| 类别 | 项 | 计价 |
|------|----|------|
| 表面处理 | 沉金 ENIG | 大板：`张数×180`；分料板：`(料板张数×料板面积)÷(1245×1041)×180` |
| | 有铅/无铅喷锡 | 0（含加工费） |
| | OSP | **拒绝** → 人工报价 |
| 颜色 | 绿色 | 0 |
| | 白/黑/蓝/黄/红/哑黑/紫/粉 | 22 元/㎡ × 实际用板面积（大板：张数×1.296045㎡；料板：张数×0.15146625㎡） |
| | 工业灰/透明/哑光灰/哑光白 | 30 元/㎡，**小批量再加 50 → 80 元/㎡** |
| 通信 | 蓝牙 / 2.4G / 有线 | +22 / +45 / 0 元每 PCS（多选） |
| 焊接 | 热插拔 | +`键数×0.25` 元/PCS（键数=配列键数，UI 实时显示） |
| | Solder（原"贴片 SMD"改名） | 0 |
| 外设 | 旋钮 Encoder（用户填数量） | +`数量×0.5` 元/PCS |
| | OLED | **拒绝** → 人工报价 |
| Logo | Kindlestar / 自定义 / 无 | 0 / **+100 元/订单** / **+400 元/订单**（v2.5.0：无改收 400） |
| 三防 | 三防漆 / MCU 灌封胶 / 标准 | **拒绝** / +5 元/PCS / 0 |
| 测试 | 全检 / 抽检 QC / 不测试 | +6 元/PCS / +`下单数量×0.3×8` 元/单（**<30 PCS 自动按全检**）/ 0 |
| 包装 | 泡沫袋/塑料袋/静电袋 | +0.3 元/PCS（单张/双张包袋选项已删除） |

### ③ 固定成本（不论订单多少必加，每 PCS）
- MCU 芯片：+15.85 元/PCS
- 外围器件：+6 元/PCS
- 二极管：+`键数×0.05` 元/PCS

### ④ RGB（PCB 编辑器联动）
- PCB 编辑器勾选「RGB孔」（`needLed`）→ 自动 +`键数×0.23` 元/PCS
- 通过 `EditorPage` 传 `rgbEnabled={projectPcbConfig.needLed}` 实时联动

### ⑤ 贴片费
- 下单数量 < 85：一次性 +650 元
- 下单数量 ≥ 85：+7 元/PCS（× effectiveQty）
- ⚠ 门槛按**下单数量**判断（用户 84 个也走 650，虽然 effectiveQty=85）

### ⑥ 钢网费
- 每单固定 +180 元

### ⑦ 快递费（v2.4.0 重货公式）
- 重量 kg = `max(1, effectiveQty × 0.1)`（首重 1kg 起）
- **轻货（重量 ≤ 8.5kg）**：费用 = `((重量-1) × 14 + 23) × 2`
- **重货（重量 > 8.5kg，即下单 ≥86 PCS）**：费用 = `150 + 重量×4 + 9`
- **无上限**（v2.4.0 取消 300 元封顶与"不计收"提示，始终计收；历史：v2.1.1 曾设 >300 不计收仅提示）

### ⑧ 折扣
- `discounts: []` 预留，暂不参与计算（后续给特定客户启用）

### ⑨ 终端报价倍率（v2.2.0 新增，v2.3.0 排除项）
- `terminalMultiplier`（pricing.json 顶层，当前 **2**）：**总价 = 计入倍率部分 × 倍率 + 不计入倍率部分原价**
- **不计入倍率（`noMultiplier: true`，原价加回）**：蓝牙 / 2.4G / 热插拔 / 旋钮 / 全检 / 抽检 / 泡沫袋·塑料袋·静电袋 / MCU 芯片 / RGB / 快递费
- 计入倍率：板材费 + 表面处理 + 颜色 + 外围器件 + 二极管 + Logo + 三防(灌封胶) + 贴片费 + 钢网费 + 有线/无选项
- 字段分布：OptionItemSchema/fixedCosts.mcu/rgb/shipping 均可带 `noMultiplier`（改价只需在 pricing.json 对应项加/删标记）
- `baseTotal`（计入倍率成本）/ `exclTotal`（倍率外原价）/ `rawTotal`（成本合计）→ `totalPrice = baseTotal × m + exclTotal`
- ⚠ **v2.7.0 UI 隐藏**：价格卡不再显示「成本小计/终端倍率/不计倍率」三行与逐项 +¥ 明细（成本构成不外露），仅引擎内部计算保留
- schema 缺省 1（不加倍），旧缓存配置自动补默认值

### ⑩ 额外小板（v2.6.0）
- 单选，默认「无」；选项：板载USB +5 / C5方形 +10 / S3跑道圆 +10 / 自定义小板 +15 元 **每 PCS**（× effectiveQty）
- 小字提示「配备线长不超过 100mm」+ 线长输入框（mm，生产信息，**不计价**）
- **板载USB 选中 → 线长输入框与排线类型禁用（灰色）**（板载 USB 自带接口不需要排线）；未选小板同样禁用

### ⑪ 排线（v2.6.0）
- 小字提示「匹配小板的排线，默认黑色」；黑色排线 0 元，FPC 排线 +1 元/PCS

### ⑫ 自定义固件（v2.6.0，多选可叠加）
- 定制灯效 **+400 元/订单**（按单）；定制控制网页 **+400 元/订单**（按单）；两者可同时勾选 = +800
- 定制其他设备：**不计价，仅提示**联系人工报价（黄色 notice，不阻断）

### ⑬ 自定义 PCB 走线（v2.6.0，单选）
- 圆角走线 / 斜角走线：0 元；定制走线 **+500 元设计费/订单**
- 小字提示「定制走线将提供三版设计改动」

### ⑭ 定位板报价（v2.6.0，独立于 PCB；v2.7.0 扩展）
- **位置**：定位板编辑器下方（PlateSection 内 `PlatePricingSection` 组件），不并入 PCB 报价卡
- **尺寸**：默认跟随定位板编辑器 `plateResult.width/height`；**v2.7.0 可手动修改**（输入框编辑即用手动值，「从PCB取尺寸」按钮清除手动值恢复跟随）
- **数量**：独立输入框（≥5，5 的倍数取整，不引用 PCB 数量）
- **材质**：1.6mm FR4 / 1.6mm 黑芯 / 1.5mm 阳极铝 / 1.5mm PC 透明；**全部复用 PCB 板价**（铝/PC 暂同 FR4 价 198.6/24.8，待校准）
- **计价**：复用 PCB 板材逻辑（大板/分料板模式、0.8 利用率、辅助边 5mm、超限检查、N=0 拒绝）
- **加工费**（v2.7.0）：`加工费 = max(张数 × 加工费率, minProcessFee)`，**下限 300 元/单**（`plate.minProcessFee`）
- **计价乘数**（v2.7.0）：`终端价 = 成本 × multiplier`（`plate.multiplier` = **2.5**，仅定位板），单价 = 终端价 ÷ 数量
- **显示**（v2.7.0）：**默认折叠**（点击按钮展开，与 PCB 报价页交互一致）；**单价加粗大号**、总价小字；**不显示成本构成**（无成本小计/乘数行）；复制文本独立段落（无成本行）
- **人工报价按钮**（v2.7.0）：Discord（`contacts.discord`，跳转 kindlestarkevin 账户）+ Email（`contacts.email`，X709208969@gmail.com）

### 输出
```
总价(终端) = (板材费+工艺费-排除项) × terminalMultiplier + 排除项原价
单价 = 总价 ÷ effectiveQty
（定位板报价独立计算、独立显示，不并入上式）
```

## 4. 用户原始需求原文（完整记录，供追溯）

### 4.1 板料计价
> "1000pcs的计价基础: 1.板材面积需要按照开板方式计算,我们正常的板材整张板是1245*1041mm"

> "按照打版尺寸算 假设我们单个板子的尺寸是344.48*119.45那么我们需要给这个板子加上上下5mm的辅助边因此计算基础就变为344.48*129.45,我们辅助边永远加在短边上,然后单边要加1.6mm的间隙避免与拼板内其他板重叠,之后计算我们用总面积乘以0.8得到实际制板面积按照20%板材浪费来计算,然后用实际制板面积除以这单个小板的面积,就获得了单张料板能够制作多少张小板的数量这个数量如果非整数则减去小数点后面的按较小数量计算, 以344.48*129.45mm的成品板尺寸计算一般一张大板只能出料24张板,这个计算方式在30pcs以上数量都通用"

> "A按照公式计算得23张可行,大板单价为黑芯板294元每张FR4普通板为198.6元每张,单张大板的加工费也按294元计,这里的计算基础都是2层板,现在我们计价基础先只提供两层板一种选项.后期增加多层或单层板我们再调整.大板单价不随数量变动. 30pcs以下数量则全部没有大的整板算而是按照我们分好料的料板487.5*310.7*0.8得到实际可制板面积然后按照这个面积除以344.48*129.45mm来得到实际的单张板能制作的数量,数量这里也一样是取整数舍弃小数点.分料板我们按照黑芯单板36.75元板费,FR4按照单张料板24.8元计算,计算加工费按照36.75元计算,如果pcb单张板子的长宽超过我们料板的单边的0.8倍则系统不再计价而是添加提示信息要求客户联系客服获取单独核算报价. 以上是板料的计价方式"

> "1.以上理解无误 2.超限规则只用于小批量 3.数据校验保留≥5 且 5 的倍数,多于此数记为报废但仍算成本. 下面是表面处理的计价方式描述1.当用户选择沉金工艺时,我们按照其订单实际消耗的板料面积计算,一张1041*1245的大板要加沉金费用为180元,例如客户下单10pcs344.48×129.45面积的板我们按分料板来制作也就是5张分料板可以做完这个订单,则我们按照1041*1245/(487.5*310.7*5)*180=要加的沉金费用 这个费用计算也是以30pcs为分水岭两种计价方式 2.当用户选择有铅喷锡及无铅喷锡时不加总价这算为pcb制版加工费的一部分 3.当用户选择osp工艺时提示计价系统失效请联系人工报价 4.我们目前遗漏了颜色选择,请帮我加上,当用户选择绿色油墨时不计费用,当用户选择白色\黑色\蓝色\黄色\红色\哑黑\紫色\粉色等颜色时每个平方料板面积按22元加价 当用户选择工业灰\透明\哑光灰\哑光白颜色时每平方料板按30元计算这四种颜色低于30pcs订单选择时每平方料板面积加50元颜色费"

> "1.对 2.5 张料板 = 0.584 张大板 → 0.584 × 180 = 105 元 这个是对的方向 3.对 4.沉金在大批量时直接按照大板张数 × 180 加费用."

### 4.2 功能项计价
> "通信方式蓝牙模块 —— 按 PCS 加22元；2.4G 无线模块 —— 按 PCS 加45元；有线 —— 0（默认）
> ② 轴体连接热插拔 —— 按照编辑器键盘画布区下方计算的keys数量*0.25元来得到每pcs加价多少；贴片 SMD —— 此项应该改名为solder 默认不加价
> ③ 外设旋钮 Encoder —— 这里给用户一个输入框让他填写encoder数量,单个encoder加价0.5元每pcs；OLED 屏幕 —— 用户选择此项后提示联系人工报价
> ④ Logo 标识Kindlestar 丝印 ——默认不加价；自定义 Logo —— 每个订单加价100元而不是每pcs加价；无 —— 0
> ⑤ 三防处理三防漆 —— 客户选择后提示人工报价；MCU 灌封胶 —— 按每pcs加价5元；标准板（无防护）—— 0
> ⑥ 测试选项全检 —— 按每pcs加价6元；抽检 QC —— 按总订单数量*0.3*8计算整个订单要加的费用,低于30pcs订单默认全检；不测试 —— 0
> ⑦ 包装泡沫袋 / 塑料袋 / 静电袋 ---按每pcs 0.3元计价；单张包袋 / 双张包袋 —— 各按 PCS 多少？"

> "⑦ 包装泡沫袋 / 塑料袋 / 静电袋 ---按每pcs 0.3元计价；单张包袋 / 双张包袋 —— 这两个选项删除
> ⑧ 其他数量折扣阶梯 ——后续我们需要给特定客户提供折扣阶梯但是现在暂不计算；板厚 —— 用户目前只能选1.2mm或1.6mm其他选项删除 板厚不影响计价"

### 4.3 固定成本/贴片/钢网/快递
> "继续优化报价:1.不论多少订单我们都要给每pcs加:15.85元的mcu费用/6元的外围器件费用/keys数量*0.05元的二极管费用 2.如果用户在上方pcb编辑器中选择了RGB 则需要每pcs加keys数量*0.23元的RGB费用 3.每个订单数量低于85pcs时按一次性加收650元贴片费 数量超过85pcs则单按单片数量*7算每pcs加7元贴片费 4.每个订单默认总价我们需要加180元钢网费 5.每个订单都要加快递费,按照数量*100g获取基础重量然后转换为kg单位 ((重量-1)*14+23)*2=快递费 当快递费超过500时提示超重货物联系人工获取报价"

### 4.4 快递费修正
> "快递费用的计算有点问题我们这样做,当客户的快递费超过300时我们不计算快递费而是在页面上显示当前未收取快递费,详情请联系人工报价,但是这个不要再影响实时报价的显示"

## 5. 关键决策与踩坑记录（接手必看）

1. **弃用参考项目公式**：面积阈值 1㎡ 会致 37 把后单价暴跌 114 倍，改按开板方式
2. **0.8 系数与 24 张的差异**：公式算 344.48×129.45 得 N=23.25→23，用户确认按公式（0.8）计算，24 是经验值仅参考
3. **沉金公式方向**：用户最初写反（分子大板），确认后为 `料板面积÷大板面积×180`（10 PCS 例 = 105 元而非 308）
4. **贴片费门槛用下单数量**（`d.quantity`）而非有效数量（84 走 650，虽然 effectiveQty=85）
5. **快递费 >300** 是 notice（非阻断）而非 reject；**OSP/OLED/三防漆/超限** 是 reject（阻断，提示人工）
   ⚠ **v2.6.0 变更**：所有**选项类**"需人工报价"（OSP/OLED/三防漆/固件·其他设备）改为**仅提示不阻断**（notice 黄条，价格照常显示）；
   **尺寸超限**（分料板超限/N=0）仍为阻断 reject（物理上无法排版，必须人工）
6. **终端报价倍率**（v2.2.0 → v2.3.0）：`terminalMultiplier` 放 pricing.json（用户可自助改价，改完 bump 版本），
   引擎 `totalPrice = baseTotal × m + exclTotal`；**排除项**（蓝牙/2.4G/热插拔/旋钮/测试/包装/MCU/RGB/快递）在 pricing.json 对应项打 `noMultiplier: true` 标记，原价加回；
   明细带 `noMultiplier` 标记；schema 全 optional 兼容旧缓存；UI 加「成本小计/终端倍率/不计倍率」三行
   ⚠ **v2.6.0 扩展**：新增 5 类费用（额外小板/排线/固件/走线/定位板）全部 `noMultiplier`（用户决策：这些费用不翻倍）
6. **RGB 联动**：`rgbEnabled` 从 EditorPage 传入，计算时合并进 form（`{...form, rgb: rgbEnabled}`），避免 useEffect setState（react-hooks/set-state-in-effect 规则）
7. **板框尺寸唯一数据源**：计价尺寸**默认自动跟随** PCB 编辑器成品板框（`computePCBBounds` 抽为公共函数，generatePCB 内部复用）。
      含 edgeDistance 边距、旋转感知边界（旋转键角点）、组件扩展（4P/MCU），与 PCB 预览显示完全一致；弃用 `layoutBounds`（键包围盒，系统性偏小且无组件/旋转信息）。
      form 尺寸 0 = 跟随 PCB（派生显示，无 useEffect setState，规避 react-hooks 规则）；用户手动编辑后用手动值；
      「从PCB取尺寸」按钮 = 清除手动值恢复跟随；空配列时按钮禁用并提示
8. **jsDelivr 无 SHA**：data.jsdelivr.com 对 gh 分支不返回 commit SHA（实测），故更新链路 = raw 优先 → jsDelivr@main 兜底，旧内容由版本递增强校验拒绝
9. **i18n**：只写 en/zh/zh-TW（运行时 `entry[lang] ?? entry.en ?? key` fallback）；配置项名称（颜色/材料等）直接取自 pricing.json 的 name 字段不翻译
10. **数量校验**：<5 报错；非 5 倍数不报错，向上取整计入报废（价格卡显示）
11. **lint 存量债务**：CI 中 lint 为 continue-on-error（~600 项历史错误），新代码必须零错误；`react-hooks/refs`、`no-restricted-syntax`（EditorPage 内联色）是既有债务勿动
12. **v2.7.0 定位板价格构成**：`成本 = 张数×材质料板价 + max(张数×加工费率, 300)`；`终端价 = 成本 × 2.5`；
    用户反馈 5 张样品单价 29.4 过低 → 乘数 2.5 + 加工费下限 300 双管齐下（示例：285×95 黑芯 5 张 → 成本 373.5 → 总价 933.75 → 单价 186.75）
13. **v2.7.0 人工报价按钮**：Discord/Email 链接放 pricing.json `contacts`（改联系渠道无需改代码）；Discord 链接为配置项，当前 `https://discord.com/users/kindlestarkevin`（username 格式，若需数字 ID/邀请链接改配置即可）
14. **v2.7.0 成本不外露**（用户要求）：页面/复制文本都不显示成本明细（PCB：无板费/工艺费/逐项+¥/成本小计/倍率三行；定位板：无成本小计行；表单选项无加价小字）。引擎 QuoteResult 仍完整保留成本字段（boardCost/rawTotal/baseTotal/exclTotal/breakdown），仅 UI 不渲染——测试不受影响

## 6. 改价流程（日常维护）

1. 打开 https://github.com/709208969/kindlestar-pricing → `pricing.json` → 铅笔编辑
2. 改数值 + **`_meta.version` 递增**（如 2.1.1 → 2.2.0）+ `updatedAt` 当天
3. Commit（main 分支）
4. 用户端「更新价格」→ 版本校验 + diff 预览 → 确认应用 → localStorage 缓存生效
5. ⚠ 忘了 bump 版本 → 工具报"版本不高于本地"拒绝应用（防呆设计）

## 7. 验证

```bash
npx vitest run tests/price-calculator.test.ts   # 70 用例（v2.6.0 起）
npx vitest run tests/pricing-loader.test.ts     # 17 用例
npx vitest run                                  # 全量 483 passed
npm run typecheck
npm run build                                   # 注意：build 会 bump 版本（sync-version 默认 --bump）
```

手测验证点（344.48×119.45 例）：
- 10 PCS FR4 喷锡绿色 → 分料板 ×5 张（每张 2 块），板费 307.75；工艺费含 MCU 158.5+外围 60+二极管 52+贴片 650+钢网 180+快递 46
- 数量 100 → 大板模式；数量 100（10kg>8.5）→ 快递重货 199 元照收；数量 85（8.5kg 边界）→ 旧公式 256 元
- 选 OSP/OLED/三防漆 → 拒绝报价提示人工；PCB 勾 RGB → RGB 费实时增加（keyCount 跟随当前配列）

## 8. 待办 / 未决事项

### ✅ 六项计费（2026-08-28 已定稿实现，v2.6.0）

> 2026-08-27 晚口述的 6 项计费，2026-08-28 全部完成：
> - [x] **额外小板**：板载USB +5 / C5方形 +10 / S3跑道圆 +10 / 自定义小板 +15 元/PCS（单选默认无）；小字「配备线长不超过 100mm」+ 线长输入框（不计价）；板载USB 时线长/排线禁用（§3-⑩）
> - [x] **排线**：黑色 0 / FPC +1 元/PCS；小字「匹配小板的排线，默认黑色」（§3-⑪）
> - [x] **自定义固件**：灯效 +400/单、控制网页 +400/单（可叠加）；定制其他设备仅提示不阻断（§3-⑫）
> - [x] **自定义 PCB 走线**：圆角/斜角 0，定制 +500 设计费/单 + 小字「提供三版设计改动」（§3-⑬）
> - [x] **USB**：归入额外小板的「板载USB」选项（+5 元/PCS），无独立 USB 项
> - [x] **定位板报价**：定位板编辑器下方独立区块，尺寸跟随编辑器、独立数量，材质复用 PCB 板价（§3-⑭）

### 🟡 业务确认项

- [ ] **铝/PC 定位板价格**：暂按 FR4 价（198.6/24.8 + 加工费 294/36.75），实际铝合金/PC 板材成本差异大，需用户补充真实单价（pricing.json `plate.materials`）
- [ ] **Discord 链接确认**：`contacts.discord` 当前为 `https://discord.com/users/kindlestarkevin`（username 格式）。Discord 用户页需数字 ID 或邀请链接，若点开 404 请提供正确链接（改 pricing.json 即可）
- [ ] **定位板超限规则**：目前复用 PCB 分料板超限（390×248.56mm），定位板更大更常见，若大量触发需评估单独放宽
- [ ] **价格参数校准**：当前数值为用户口述（视为真实报价依据），正式上线前建议用户复核
- [ ] **Logo「无」收 400 元/单确认**：不印 Logo 反而最贵，且计入终端倍率（×2 → 800 元）。
      若不该翻倍需给 logo 项加 `noMultiplier`；若金额不对直接改 pricing.json
- [ ] **MCU 15.85 元对应型号确认**：nRF52832/STM32/其他？不同方案成本差 3-5 倍
- [ ] **0.8 利用率系数 vs 真实排版**：用户经验 24 张 vs 公式 23 张。0.8 是经验值，建议用真实排版图验证一次
- [ ] **大板模式对异形配列适用性**：N 随面积线性缩放，但细长板（700×50mm）实际出板数可能远小于公式

### 🟢 后续扩展

- [ ] **多层/单层板**：目前仅 2 层板，后续增加需扩展 pricing.json（`materials` 加层数字段或独立结构）
- [ ] **折扣阶梯**：`discounts: []` 已预留，后续为特定客户启用（引擎需加 discount 应用逻辑）
- [ ] **报价历史记录**：同一配置在不同价格版本下的对比（价格变动对老客户报价追溯）
- [ ] **实际成本回流校准**：真实订单制板成本 → 反推校准参数的闭环（目前参数只能拍脑袋）
- [ ] **报价单导出**：目前只有复制文本，可加导出/存档
- [x] **终端报价倍率**（2026-08-27）：v2.2.0 新增 `terminalMultiplier=2`，总价 = 成本合计 ×2（引擎/UI/测试/仓库已同步）
- [x] **倍率排除项**（2026-08-27）：v2.3.0 蓝牙/2.4G/热插拔/旋钮/全检/抽检/包装/MCU/RGB/快递 打 `noMultiplier` 标记不计入倍率，
      总价 = 计入倍率部分×2 + 排除项原价；测试 +2（排除项/向后兼容），pricing-loader 测试版本号改 99.9.9 防未来冲突
- [x] **快递费重货公式 + 取消上限**（2026-08-27）：v2.4.0 重量>8.5kg（≥86 PCS）改 `150+重量×4+9`，
      删除 maxAutoFee/overweightMessage 上限逻辑（notice 机制保留给未来场景）；测试 +1（无上限照收）
- [x] **Logo 计价调整**（2026-08-27）：v2.5.0 无 +400 元/单、Kindlestar 丝印 0 元、自定义 100 元/单（按订单，不计 PCS）；测试改为相对 Kindlestar 断言
- [x] **六项计费定稿**（2026-08-28）：v2.6.0 额外小板/排线/固件/走线/定位板 + 人工报价改仅提示
- [x] **定位板 v2.7.0 扩展**（2026-08-28）：手动尺寸 / 计价乘数 ×2.5（仅定位板）/ 加工费下限 300 / Discord+Email 按钮 / 默认折叠 / 页面隐藏成本构成 / 复制文本精简
- [ ] 交接时版本：编辑器 v1.0.38（build 自动 bump），价格清单 **v2.7.0**

## 9. 给接手模型的开场指引

1. 先读本文件第 3 节（规则）与第 5 节（决策），再读 `src/lib/price-calculator.ts` 与 `src/components/PricingSection.tsx`
2. **2026-08-28 已完成 §8 六项计费（v2.6.0）+ 定位板 v2.7.0 扩展**：额外小板/排线/固件/走线/USB（板载USB 选项）/定位板全部落地；定位板手动尺寸、×2.5、加工费下限、Discord/Email 按钮
3. **后续最可能的方向**：铝/PC 定位板真实单价校准（pricing.json `plate.materials`）、Discord 链接确认（`contacts.discord`）、人工报价项提示文案、折扣阶梯启用
4. 改完必跑：`npx vitest run tests/price-calculator.test.ts` + `npm run typecheck`
5. 涉及 UI 用 `--theme-*` 变量，i18n 加 en/zh/zh-TW 三语
6. 仓库推送：`C:\Users\KEVIN-~1\AppData\Local\Temp\opencode\kindlestar-pricing`（本地 git 已连远程；v2.6.0 已推送）
7. 提交纪律：git 操作前确认目录；不在 master/main 直接提交；commit 用 Conventional Commits

---

## 10. 预演分析记录（2026-08-27 晚，含三轮）

> 用户要求对"计价方式"做失败预演，结论沉淀于此，供后续防患。

### 10.1 第一轮（实现前，v2 计划阶段）
- 参考项目"面积阈值 1㎡"公式对键盘业务致命（37 把后单价暴跌 114 倍）→ 已改按开板方式（§5-1）
- 沉金公式方向用户最初写反 → 已确认（§5-3）
- 0.8 系数与经验值 24 张矛盾 → 用户确认按公式（§5-2）

### 10.2 第二轮（功能项定价）
- 贴片费门槛按下单数量而非有效数量（§5-4）
- 快递 >500 原计划 reject，用户改 300 且不阻断（§5-5，v2.1.1）

### 10.3 第三轮（本轮，实现完成后）
发现 2 处真实代码缺陷 + 1 处系统性偏差，均已入 §8 🔴 待办：
1. 旋转输入超限误判（归一化缺失）→ **已修复（2026-08-27）**：超限检查归一化为 max/min，新增 3 用例
2. N=0 板材费 ¥0 → **已修复（2026-08-27）**：`N<=0` 拒绝计价（`BOARD_TOO_LARGE_MSG`），新增 2 用例
3. 配列取尺寸不含 PCB 边距 → 板材费系统性偏低 → 已修复（2026-08-27 方案①：取尺寸自动加边距）
   **用户随后追加要求：尺寸只从 PCB 编辑器获取** → 改为 `computePCBBounds`（pcb-export.ts 公共函数，generatePCB 复用），
   PricingSection 经 `pcbSize` prop 接收，按钮禁用逻辑 + i18n 更新，pcb-export.test.ts +5 用例
其余风险（参数未经订单验证、双份配置漂移、快递 300 缺口、0.8 系数、MCU 型号）入 §8 🟡
